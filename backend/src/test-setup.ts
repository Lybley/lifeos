// Global test setup
// Add any global test configuration here

// Example: Set longer timeout for all tests
jest.setTimeout(10000);

// Example: Mock environment variables
process.env.NODE_ENV = 'test';
